# -*- coding: utf-8 -*-
"""CTF.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/146GvGxfk1CX14utohjsiFlGYUqtTooaF

**กติกาการเล่น**


---
1.  CTF นี้มีคำใบ้ 2 โหมดคือ Normal กับ Hard คุณสามารถเลือกได้ผ่านการ Click Link ด้านล่าง
- Normal Mode: https://drive.google.com/file/d/1dLPJGikGIi6ZQiwtgXcAFNu6K1nSxGpY/view?usp=sharing
- Hard Mode: https://drive.google.com/file/d/1Sa8UxbRKq8z8xTrTVeSjN9C_RjIYs1B-/view?usp=sharing
2. อย่างที่ทราบกัน Key นี้เป็นเพียง 1 ใน 3 Key ที่คุณต้องหา เอาละเมื่อคุณพร้อมแล้ว
- > ให้คุณ Run ส่วนของการ install ที่จำเป็นก่อน
"""

!pip install -q pycryptodome
!pip install -q ipywidgets
from IPython.display import clear_output
clear_output()

"""**ตรวจสอบคำใบ้แรก**


---


"""

# @title
import base64
from Crypto.Cipher import AES
from Crypto.Util.Padding import unpad
from getpass import getpass  # ใช้เพื่อซ่อนการกรอกรหัส

# ฟังก์ชันในการถอดรหัส
def decrypt_aes(ciphertext_b64, key_decimal):
    try:
        # แปลง Key จากเลขฐานสิบเป็น bytes
        key = key_decimal.to_bytes(16, byteorder='big')  # 16 bytes

        # ถอดรหัส Base64
        ciphertext = base64.b64decode(ciphertext_b64)

        # ตั้งค่า AES ECB Mode
        cipher = AES.new(key, AES.MODE_ECB)

        # ถอดรหัสและ Unpad
        plaintext = unpad(cipher.decrypt(ciphertext), AES.block_size)
        return plaintext.decode('utf-8')
    except Exception:
        return None

# Ciphertext (จากการเข้ารหัส)
ciphertext_key = "GJtzUwnlC1Olxgc2wMtYNw=="

# วนลูปจนกว่าจะถอดรหัสสำเร็จ
while True:
    try:
        # ใช้ getpass เพื่อซ่อนการกรอกรหัส
        key_decimal = int(getpass("กรุณากรอก Key (เลขฐานสิบ จำนวน 24 ตัว): "))

        # ถอดรหัส
        result = decrypt_aes(ciphertext_key, key_decimal)

        # ตรวจสอบผลลัพธ์
        if result == "Correct":
            print(f"✔️ Keyword ที่คุณหามาได้ถูกต้อง")
            break
        else:
            print("❌ Keyword ที่คุณหามาได้ถูกต้องไม่ถูกต้อง! ลองใหม่อีกครั้ง.")
    except ValueError:
        print("กรุณากรอก Key เป็นตัวเลขฐานสิบที่ถูกต้อง!")

"""**ตรวจสอบคำใบ้สอง**


---


"""

# @title
import base64
from Crypto.Cipher import AES
from Crypto.Util.Padding import unpad
from getpass import getpass  # ใช้เพื่อซ่อนการกรอกรหัส

# ฟังก์ชันในการถอดรหัส
def decrypt_aes(ciphertext_b64, key_decimal):
    try:
        # แปลง Key จากเลขฐานสิบเป็น bytes
        key = key_decimal.to_bytes(16, byteorder='big')  # 16 bytes

        # ถอดรหัส Base64
        ciphertext = base64.b64decode(ciphertext_b64)

        # ตั้งค่า AES ECB Mode
        cipher = AES.new(key, AES.MODE_ECB)

        # ถอดรหัสและ Unpad
        plaintext = unpad(cipher.decrypt(ciphertext), AES.block_size)
        return plaintext.decode('utf-8')
    except Exception:
        return None


ciphertext_key = "X/2rO+V8etpXLWcuxhDWPA=="

# วนลูปจนกว่าจะถอดรหัสสำเร็จ
while True:
    try:
        # ใช้ getpass เพื่อซ่อนการกรอกรหัส
        key_decimal = int(getpass("กรุณากรอก Key (เลขฐานสิบ จำนวน 6 ตัว): "))

        # ถอดรหัส
        result = decrypt_aes(ciphertext_key, key_decimal)

        # ตรวจสอบผลลัพธ์
        if result == "Correct":
            print(f"✔️ Keyword ที่คุณหามาได้ถูกต้อง")
            break
        else:
            print("❌ Keyword ที่คุณหามาได้ถูกต้องไม่ถูกต้อง! ลองใหม่อีกครั้ง.")
    except ValueError:
        print("กรุณากรอก Key เป็นตัวเลขฐานสิบที่ถูกต้อง!")

"""**ตรวจสอบ key อันสุดท้าย**


---


"""

# @title
import base64
from Crypto.Cipher import AES
from Crypto.Util.Padding import unpad
from ipywidgets import widgets, VBox, HBox
from IPython.display import display, clear_output, Javascript

# ฟังก์ชันในการถอดรหัส
def decrypt_aes(ciphertext_b64, key_input):
    try:
        ciphertext = base64.b64decode(ciphertext_b64)

        # ใช้ AES.MODE_ECB ไม่ต้องใช้ IV
        cipher = AES.new(key_input, AES.MODE_ECB)
        plaintext = unpad(cipher.decrypt(ciphertext), AES.block_size)
        return plaintext.decode('utf-8')
    except Exception:
        return None

# Ciphertext ที่ได้จากการเข้ารหัส (Base64)
ciphertext_key = "/E4lVPYMz8oFokK+HtEZQw=="
check_answer = "ngCgwwswcjcivTrAf5bvOQ=="

# widgets สำหรับอินเทอร์เฟซ
key_input_widget = widgets.Password(description="🔑 Key:", placeholder="กรอกรหัส 32 ตัว (เป็นเลขฐาน 2)")
check_button = widgets.Button(description="🚀 ตรวจสอบ", button_style='success')
output_area = widgets.Output()

# ฟังก์ชันสำหรับตรวจสอบ Key
def check_key(b):
    with output_area:
        clear_output()
        key_input = key_input_widget.value.encode('utf-8')
        result1 = decrypt_aes(ciphertext_key, key_input)
        result2 = decrypt_aes(check_answer, key_input)

        if result2 == "Correct":
            # แสดงผลลัพธ์ใน popup ด้านบน
            display(Javascript(f'alert("🎉✨ ยินดีด้วยคุณตอบถูก! ✨🎉 \\n📄 ผลลัพธ์ที่ถอดรหัสได้ -> {result1}")'))
            check_button.disabled = True  # ปิดปุ่มเมื่อคำตอบถูกต้อง
        else:
            print("         ❌ Key ไม่ถูกต้อง! 😔 ลองกรอกใหม่อีกครั้ง.")

# เชื่อมต่อปุ่มกับฟังก์ชันตรวจสอบ
check_button.on_click(check_key)

# แสดงอินเทอร์เฟซ
display(VBox([HBox([key_input_widget, check_button]), output_area]))